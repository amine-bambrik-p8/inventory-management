import { ProductEntryValuePipe } from './product-entry-value.pipe';

describe('ProductEntryValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductEntryValuePipe();
    expect(pipe).toBeTruthy();
  });
});
