import { OrderValuePipe } from './order-value.pipe';

describe('OrderValuePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderValuePipe();
    expect(pipe).toBeTruthy();
  });
});
