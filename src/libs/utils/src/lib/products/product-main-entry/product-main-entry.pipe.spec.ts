import { ProductMainEntryPipe } from './product-main-entry.pipe';

describe('ProductMainEntryPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductMainEntryPipe();
    expect(pipe).toBeTruthy();
  });
});
